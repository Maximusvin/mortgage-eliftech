import { Video } from 'components';
import { SectionWrap } from '../UI/SectionWrap';

export const VideoView = () => {
  return (
    <SectionWrap title="Video">
      <Video />
    </SectionWrap>
  );
};
